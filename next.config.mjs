/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add your existing config here, e.g.:
  images: {
    domains: ['example.com'], // if you have any
  },
  // ... other settings
};

export default nextConfig;